const config = {
	port: 3000,
	host: "localhost",
	databaseConfig: {
		host: "localhost",
		port: 3306,
		username: "root",
		password: "",
		databaseName: "saveeasily",
	},
};

export default config;
