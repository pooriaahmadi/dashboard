{
	"name": "saveeasily-dashboard",
	"version": "1.0.0",
	"description": "Dashboard backend for saveeasily bot",
	"main": "src/index.ts",
	"scripts": {
		"start": "npm run build && node ./dist/index.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"preinstall": "npm i typescript -g && npm i rimraf -g",
		"build": "rimraf ./build && npx tsc",
		"dev": "nodemon"
	},
	"author": "fuckin pooria ahmadi",
	"license": "MIT",
	"dependencies": {
		"@types/express": "^4.17.13",
		"@types/node": "^16.7.6",
		"chalk": "^4.1.2",
		"dotenv": "^10.0.0",
		"express": "^4.17.1",
		"mysql": "^2.18.1"
	},
	"devDependencies": {
		"@types/mysql": "^2.15.19"
	}
}
